PREFIX ex: <https://ecohealth-xi.vercel.app/>

CONSTRUCT {
  ?obs ex:hasRiskLevel ex:RiskLevel_High .
}
WHERE {
  ?obs a ex:AirQualityObservation ;
       ex:pm25Value ?v .
  FILTER (?v > 100 && ?v <= 150)
}
